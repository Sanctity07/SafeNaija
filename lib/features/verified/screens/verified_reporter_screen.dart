import 'package:flutter/material.dart';

class VerifiedReporterScreen extends StatelessWidget {
  const VerifiedReporterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}